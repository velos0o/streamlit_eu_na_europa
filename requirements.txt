# Bibliotecas principais para análise de dados e visualização
streamlit==1.44.0        # Framework para construção de dashboards interativos
pandas==2.2.0            # Manipulação e análise de dados
numpy==1.26.3            # Computação numérica
requests==2.31.0         # Requisições HTTP
python-dateutil==2.8.2   # Funcionalidades avançadas para datas
python-dotenv==1.0.1     # Carregamento de variáveis de ambiente
plotly==5.20.0           # Gráficos interativos avançados
matplotlib==3.8.2        # Visualização de dados estática
xlsxwriter==3.1.9        # Criação de arquivos Excel com formatação
thefuzz==0.22.1          # Matching e comparação fuzzy de strings (forma principal)
python-Levenshtein==0.27.1 # Dependência para acelerar o thefuzz
unidecode==1.3.8         # Remoção de acentos de strings
toml==0.10.2             # Manipulação de arquivos TOML (para secrets)

# Bibliotecas para integração e comunicação externa
gspread==6.2.0           # Acesso ao Google Sheets
google-auth==2.28.1      # Autenticação com serviços Google
google-auth-oauthlib==1.2.0  # Autenticação OAuth para Google
oauth2client==4.1.3      # Autenticação OAuth legacy

# Biblioteca para manipulação de imagens
Pillow==10.2.0           # Processamento de imagens

# Biblioteca para manipulação avançada de Excel
openpyxl==3.1.2          # Manipulação avançada de arquivos Excel

# Biblioteca opcional para animações de carregamento
streamlit-lottie==0.0.5  # Animações Lottie no Streamlit
# Bibliotecas para mapas interativos
folium==0.19.5           # Criação de mapas interativos
streamlit-folium==0.24.0 # Integração Folium com Streamlit

# Biblioteca para conexão MySQL
mysql-connector-python==8.3.0  # Conexão com bancos MySQL

# Bibliotecas para componentes visuais adicionais
streamlit-card==0.0.61   # Componente de cards para Streamlit
streamlit-extras==0.3.6  # Componentes extras para Streamlit

# Dependências para geocodificação e manipulação de dados geográficos
rapidfuzz==3.12.2        # Dependência opcional para thefuzz 

# Bibliotecas para tema escuro e componentes visuais avançados
streamlit-toggle-switch==1.0.2  # Toggle switch para alternar entre temas
httpx==0.27.0            # Cliente HTTP assíncrono (dependência)
extra-streamlit-components==0.1.60  # Componentes extras avançados 

streamlit-aggrid # Para tabelas interativas avançadas